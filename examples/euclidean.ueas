Algorithm EuclideanDistance(x1, y1, x2, y2)
    Require: x1: Real, y1: Real, x2: Real, y2: Real
    Ensure: Real
    Complexity: "O(1)"

    dx <- x2 - x1
    dy <- y2 - y1
    assert(dx * dx >= 0.0, "Square of real must be non-negative")
    return sqrt(dx * dx + dy * dy)
