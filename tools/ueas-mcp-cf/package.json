{
  "name": "ueas-mcp-cf",
  "version": "4.2.0",
  "private": true,
  "description": "UEAS MCP Server — Cloudflare Worker deployment",
  "scripts": {
    "build:wasm": "powershell.exe ../build_wasm.ps1",
    "predeploy": "npm run build:wasm",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
